import 'package:flutter/material.dart';

class Share_App extends StatelessWidget {
  const Share_App({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("share app "),
    );
  }
}
